console.log('Hello!');
const listTodos = [];

// Define the showPrompt function
function showPrompt() {
  let todo = prompt('Saisir la tache :');
  if (todo !== null) {
    console.log(todo);
  }
}
// const inputEvent = (e)=>{
//   console.log(e.target.value)
// }
const showResult = () => {
  //result.innerText = JSON.stringify(listTodos);
  const div = document.createElement('div');
  div.innerText=
};
